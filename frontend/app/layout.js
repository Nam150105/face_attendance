export const metadata = {
  title: "Face Attendance",
  description: "Face attendance foundation"
};

export default function RootLayout({ children }) {
  return (
    <html lang="vi">
      <body>{children}</body>
    </html>
  );
}
